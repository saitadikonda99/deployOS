# Copy to .env and adjust as needed. All variables are optional; defaults
# are shown. Environment variables always take precedence over config.yaml.

# environment: development | production
DEPLOYOS_ENVIRONMENT=development

# log_level: debug | info | warn | error
DEPLOYOS_LOG_LEVEL=info

# Address the node agent's HTTP server listens on.
DEPLOYOS_AGENT_HTTP_ADDR=:8081

# Identifies this agent within a fleet. Empty until registered.
DEPLOYOS_AGENT_ID=

# Address the control plane's HTTP server listens on.
DEPLOYOS_SERVER_HTTP_ADDR=:8080
