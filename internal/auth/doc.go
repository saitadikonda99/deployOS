// Package auth will hold authentication and authorization for DeployOS:
// verifying operators against the control plane and verifying agents
// against each other. Nothing is implemented yet - this package exists so
// the boundary has an obvious home once it's needed.
package auth
