// Package secrets will hold storage and injection of application secrets
// managed by the DeployOS control plane. Nothing is implemented yet - this
// package exists so the boundary has an obvious home once it's needed.
package secrets
